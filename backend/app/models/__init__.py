"""Pydantic schemas for API contracts and value objects.

These are the *boundary* types — what the API accepts and returns, and
value objects that get embedded as JSONB inside the ORM tables.

ORM tables live in `app.db.tables`.
"""
